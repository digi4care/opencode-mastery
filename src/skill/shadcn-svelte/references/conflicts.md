# Conflict Report

Found **173** conflicts between sources.

## Severity Breakdown

- 🔴 **High**: 0 (action required)
- 🟡 **Medium**: 173 (review recommended)
- 🟢 **Low**: 0 (informational)

## 🟡 Medium Severity

### buildRegistry

API exists in code (docs/scripts/registry.ts) but not found in documentation

### main

API exists in code (packages/cli/src/index.ts) but not found in documentation

### collectFiles

API exists in code (docs/scripts/build-llms.ts) but not found in documentation

### toMarkdown

API exists in code (docs/scripts/build-llms.ts) but not found in documentation

### findVeliteData

API exists in code (docs/scripts/build-llms.ts) but not found in documentation

### createLLMsIndex

API exists in code (docs/scripts/build-llms.ts) but not found in documentation

### formatName

API exists in code (docs/scripts/build-llms.ts) but not found in documentation

### visit

API exists in code (docs/scripts/build-llms.ts) but not found in documentation

### createFile

API exists in code (docs/scripts/build-llms.ts) but not found in documentation

### writeFileWithDirs

API exists in code (docs/scripts/build-registry.ts) but not found in documentation

### build

API exists in code (docs/scripts/build-registry.ts) but not found in documentation

### crawlUI

API exists in code (docs/scripts/registry.ts) but not found in documentation

### buildUIRegistry

API exists in code (docs/scripts/registry.ts) but not found in documentation

### crawlExamples

API exists in code (docs/scripts/registry.ts) but not found in documentation

### buildBlockRegistry

API exists in code (docs/scripts/registry.ts) but not found in documentation

### crawlBlocks

API exists in code (docs/scripts/registry.ts) but not found in documentation

### crawlLib

API exists in code (docs/scripts/registry.ts) but not found in documentation

### crawlHooks

API exists in code (docs/scripts/registry.ts) but not found in documentation

### getFileDependencies

API exists in code (packages/cli/src/commands/registry/deps-resolver.ts) but not found in documentation

### enter

API exists in code (packages/cli/src/commands/registry/deps-resolver.ts) but not found in documentation
